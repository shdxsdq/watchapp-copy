let $style$1182111904 = {
  "@info": {
    "styleObjectId": 1182111904
  }
};
const $app_style$1182111904 = $style$1182111904;
const LOCAL_ARTICLES = [{
  "id": "local-58771f595f",
  "title": "玻璃森林的夏天",
  "summary": "本地 TXT",
  "readTime": "7 条",
  "paragraphs": [{
    "text": "《玻璃森林的夏天》\n北方小城连续下了四十九天雨。第五十天清晨，郊外长出一片透明森林，树干像玻璃，叶片在风中发出清脆的铃声。人们担心碰碎它，只敢站在远处观看。\n少年闻野却独自走了进去。他发现每棵树里都封存着一个夏天：西瓜裂开的声音、晒热的石阶、傍晚飞舞的蜻蜓，还有自行车轮碾过水洼时溅起的光。\n森林深处有一棵没有颜色的小树。树心里坐着一个女孩，她说自己叫晴朗，是这座城被雨困住的最后一个夏日。只有当城里的人愿意分享一段快乐的记忆，她才能重新醒来。\n闻野跑回城中，敲开一扇又一扇门。起初人们只谈论潮湿和阴霾，后来，一位老奶奶想起年轻时露天电影的白幕，一个面包师想起父亲教他揉面的午后，孩子们则想起去年追逐泡泡的广场。\n这些记忆化作金色光点，越过屋顶，飞进透明森林。小树一点点染上绿色，晴朗推开树干走出来。云层裂开一道缝，阳光落下时，整片森林融成无数清凉的水滴。\n那年夏天只剩三天，却成了小城最长久的季节。此后每逢阴雨，人们都会讲起快乐的往事，因为他们知道，阳光有时不是从天空出现，而是从彼此的记忆里升起。",
    "color": ""
  }]
}, {
  "id": "local-19a4f582b8",
  "title": "倒着走的钟",
  "summary": "本地 TXT",
  "readTime": "7 条",
  "paragraphs": [{
    "text": "《倒着走的钟》\n旧火车站大厅中央悬着一只倒着走的钟。秒针向左，分针向后，每到午夜，它都会把无人认领的遗憾送回一分钟以前。\n值夜员周迟守了这只钟二十七年。他见过旅客追回掉落的车票，见过母亲在关门前抱住贪玩的孩子，也见过一句刻薄的话回到嘴边，最终变成沉默。可钟从来不替同一个人倒退第二次。\n一个暴雨夜，末班车即将离站，年轻的小提琴手匆匆跑进大厅。她的琴盒空着，那把陪伴她多年的琴遗落在出租车上。钟声响起，她却没有请求时间倒退，而是问周迟：“你有没有一句来不及说的话？”\n周迟望向站台尽头。二十七年前，他和弟弟在那里争吵，弟弟独自登上列车，从此杳无音信。他一直守着车站，以为等待足够久，过去就会自己回来。\n倒计时只剩十秒，周迟终于说出弟弟的名字。钟猛地停住，随后所有指针同时向前。广播里传来列车到站的提示，一个拎着旧行李箱的老人走下车，眉眼与记忆中的少年一模一样。\n那晚以后，大厅的钟恢复了正常方向。小提琴手也在失物招领处找回了琴。人们说奇迹耗尽了钟的魔法，周迟却知道，真正让时间前进的，从来不是遗忘，而是终于愿意面对。",
    "color": ""
  }]
}, {
  "id": "local-d660d980d9",
  "title": "第七码头",
  "summary": "本地 TXT",
  "readTime": "7 条",
  "paragraphs": [{
    "text": "《第七码头》\n地图上只有六码头，但老水手们都知道，涨潮后的第十三个夜晚，海面会出现第七码头。它没有航标，也没有管理员，只停靠那些迷失方向的船。\n年轻船长许澜在一次浓雾中误入那里。码头边系着各个年代的船，有褪色的木帆船、锈迹斑斑的货轮，还有一艘纸折的小艇。每条船上都没有人，甲板却保持着刚刚离开的模样。\n许澜在候船室遇到一个提灯的男孩。男孩告诉她，这些船并非沉没，而是船上的人忘记了出发的理由，于是航程停在了这里。想要离开，只需重新说出自己为何远航。\n许澜曾以为答案很简单：去更远的海，成为最年轻的远洋船长。可面对无边雾色，她想起父亲送她登船时的背影，也想起每次归港后母亲留在窗口的灯。她终于说：“我出发，是为了带着新的故事回来。”\n话音落下，候船室的门被海风推开。所有沉默的船同时亮起灯火，纸艇变成一只白色海鸟，绕着桅杆飞了三圈。男孩指向雾中一条闪光的航道，笑着说：“记得回来，远方才有意义。”\n黎明时，许澜的船重新出现在六码头外。航海日志里多了一页陌生海图，第七码头的位置却是一片空白。她没有试着寻找，因为她知道，只有真正迷失的人才会再次看见它。",
    "color": ""
  }]
}, {
  "id": "local-2229f991e7",
  "title": "雾港来信",
  "summary": "本地 TXT",
  "readTime": "6 条",
  "paragraphs": [{
    "text": "《雾港来信》\n凌晨四点，海雾漫过防波堤，把港口的灯一盏盏藏了起来。修船匠林舟在工具箱底部发现一封没有邮票的信，信封上只写着一句话：请在潮水转向前，把它交给十年后的我。\n落款是林舟自己的名字，笔迹却比现在苍老许多。他起初以为是谁的玩笑，直到信中准确写出了旧钟楼将在五点零七分停摆。时间一到，钟声果然只敲了四下，整座雾港陷入短暂的寂静。\n信里还说，东码头那艘废弃多年的蓝色拖船会在黎明前亮起舷灯。林舟穿过湿漉漉的石路赶去，看见船舱里坐着一个满头白发的老人。老人没有回头，只把一本航海日志放在窗边。\n日志记录着未来十年的风暴、离别与重逢，最后一页却是空白。老人说：“知道结局的人，最容易忘记选择本身。”随后舷灯熄灭，拖船在雾中消失，仿佛从未存在。\n潮水开始转向时，林舟没有拆开信的最后一层夹页。他把它装进新的信封，写下同样那句话，藏回工具箱底部。远处的钟楼重新走动，清晨的第一声汽笛穿过海雾，像一封刚刚启程的回信。",
    "color": ""
  }]
}, {
  "id": "local-019e219b5e",
  "title": "月亮修理铺",
  "summary": "本地 TXT",
  "readTime": "6 条",
  "paragraphs": [{
    "text": "《月亮修理铺》\n城南最窄的巷子里有一家只在阴天营业的小店。门牌写着“月亮修理铺”，橱窗中摆满银色齿轮、玻璃碎片和装在瓶子里的微光。店主阿满总说，世上的月亮并非只有一轮，每个人心里都藏着一轮小小的月亮。\n有人带来失去光泽的月亮，有人抱着裂成两半的月亮，还有人只剩下一把清冷的月尘。阿满从不询问裂痕的来历，只会沏一壶淡茶，耐心地把碎片拼回原处。\n一天，女孩小葵捧来一个空盒子。她说自己的月亮不见了，盒里只剩下海浪的声音。阿满听了很久，发现那并不是海浪，而是一句被反复咽回去的道歉。\n他们沿着声音来到河边，在一棵老柳树的影子里找到了那轮月亮。它很小，湿漉漉的，正因为无人理解而发着微弱的蓝光。小葵蹲下来，终于把那句道歉认真说完。\n月亮立刻升高了一点，照亮河面，也照亮了她回家的路。阿满没有收钱，只从盒子里取走一小段浪声，挂在店门口当作风铃。从那以后，每当有人鼓起勇气说出真心，巷子里便会响起温柔的潮汐。",
    "color": ""
  }]
}, {
  "id": "local-c3d660c3fe",
  "title": "云上邮局",
  "summary": "本地 TXT",
  "readTime": "6 条",
  "paragraphs": [{
    "text": "《云上邮局》\n群山最高处有一间白色邮局，屋顶常年停着云。这里不寄普通信件，只接收那些无法送达的话：写给童年的秘密，写给远方的思念，写给已经错过的人。\n邮差小禾每天清晨把信装进纸飞机，等待上升气流将它们送往不同的天空。每封信抵达时，收信人不会看见文字，只会在某个寻常瞬间忽然想起一阵熟悉的气味、一首旧歌，或一个久违的拥抱。\n有一天，邮局收到一封没有收信人的信。纸上写着：“希望你已经找到了想走的路。”小禾辨认出那是自己小时候的笔迹。她曾在山脚迷路，被一位陌生老人带回家；临别前，老人让她给未来写封信。\n小禾把信折成纸飞机，却迟迟没有放手。她忽然明白，自己留在邮局并不是因为无处可去，而是喜欢替那些无声的话找到归宿。眼前这条通往云端的路，就是她一直寻找的答案。\n她给小时候的自己写了回信：“路不一定通向远方，也可以通向你愿意守护的地方。”两架纸飞机在风中交错，一架飞往过去，一架飞向黎明。山下的人抬头时，只看见两片云轻轻碰在一起。",
    "color": ""
  }]
}];
const ARTICLES = LOCAL_ARTICLES;
function getArticle(articleId) {
  for (let index = 0; index < ARTICLES.length; index += 1) {
    if (ARTICLES[index].id === articleId) {
      return ARTICLES[index];
    }
  }
  return ARTICLES[0];
}
const storage = $app_require$("@app-module/system.storage");
const STATE_KEY = "watchreader.state.v1";
const CONTENT_VERSION = 2;
let cachedState = null;
function createDefaultState() {
  return {
    favorites: {},
    progress: {},
    scrollProgress: {},
    contentVersion: CONTENT_VERSION,
    deletedArticles: {},
    importedArticle: null,
    fontSize: "30",
    lineSpacing: "16",
    fontWeight: "normal",
    textAlign: "left",
    textColor: "warm"
  };
}
function loadReaderState() {
  if (cachedState) {
    return cachedState;
  }
  const defaultState = createDefaultState();
  try {
    const savedValue = storage.getSync({
      key: STATE_KEY
    });
    if (typeof savedValue !== "string" || savedValue.length === 0) {
      cachedState = defaultState;
      return cachedState;
    }
    const savedState = JSON.parse(savedValue);
    const fontSizeMap = {
      small: "26",
      medium: "30",
      large: "36"
    };
    const lineSpacingMap = {
      compact: "14",
      normal: "16",
      relaxed: "18"
    };
    cachedState = {
      favorites: savedState.favorites || {},
      progress: savedState.contentVersion === CONTENT_VERSION ? savedState.progress || {} : {},
      contentVersion: CONTENT_VERSION,
      scrollProgress: savedState.contentVersion === CONTENT_VERSION ? savedState.scrollProgress || {} : {},
      deletedArticles: savedState.deletedArticles || {},
      importedArticle: savedState.importedArticle || null,
      fontSize: fontSizeMap[savedState.fontSize] || savedState.fontSize || "30",
      lineSpacing: lineSpacingMap[savedState.lineSpacing] || savedState.lineSpacing || "16",
      fontWeight: savedState.fontWeight || "normal",
      textAlign: savedState.textAlign || "left",
      textColor: savedState.textColor || "warm"
    };
    return cachedState;
  } catch (error) {
    console.warn("读取阅读状态失败，将使用默认状态", error);
    cachedState = defaultState;
    return cachedState;
  }
}
function saveReaderState(state) {
  cachedState = state;
  storage.set({
    key: STATE_KEY,
    value: JSON.stringify(state),
    fail(data, code) {
      console.warn("保存阅读状态失败", data, code);
    }
  });
}
$app_require$("@app-module/system.interconnect");
const file = $app_require$("@app-module/system.file");
const IMPORTED_ARTICLE_ID = "imported-txt";
function getImportedArticleMetadata() {
  return loadReaderState().importedArticle;
}
function splitTxtIntoParagraphs(text) {
  const normalized = String(text || "").replace(/\r\n/g, "\n").replace(/\r/g, "\n").trim();
  if (!normalized) {
    return ["这个 TXT 文件没有可显示的文字。"];
  }
  let paragraphs = normalized.split(/\n\s*\n/).map((paragraph) => paragraph.replace(/\n+/g, " ").trim()).filter((paragraph) => paragraph.length > 0);
  if (paragraphs.length === 1 && normalized.indexOf("\n") >= 0) {
    paragraphs = normalized.split(/\n+/).map((paragraph) => paragraph.trim()).filter((paragraph) => paragraph.length > 0);
  }
  const readableParagraphs = [];
  for (let index = 0; index < paragraphs.length; index += 1) {
    const paragraph = paragraphs[index];
    if (paragraph.length <= 260) {
      readableParagraphs.push(paragraph);
      continue;
    }
    for (let offset = 0; offset < paragraph.length; offset += 260) {
      readableParagraphs.push(paragraph.slice(offset, offset + 260));
    }
  }
  return readableParagraphs;
}
function readImportedArticle(metadata, callbacks) {
  if (!metadata || !metadata.fileUri) {
    if (callbacks && callbacks.fail) {
      callbacks.fail("没有已导入的 TXT", -1);
    }
    return;
  }
  file.readText({
    uri: metadata.fileUri,
    success(data) {
      const article = {
        id: IMPORTED_ARTICLE_ID,
        title: metadata.title,
        summary: metadata.summary,
        readTime: metadata.readTime,
        paragraphs: splitTxtIntoParagraphs(data.text)
      };
      if (callbacks && callbacks.success) {
        callbacks.success(article);
      }
    },
    fail(data, code) {
      if (callbacks && callbacks.fail) {
        callbacks.fail(data, code);
      }
    }
  });
}
const ARTICLE_CACHE = {};
const $app_script$1182111904 = {
  data: function dataFun() {
    return {
      articleId: "",
      article: {
        id: "",
        title: "",
        summary: "",
        readTime: "",
        paragraphs: []
      },
      pendingParagraphs: [],
      readerState: {
        favorites: {},
        progress: {},
        scrollProgress: {},
        fontSize: "30",
        lineSpacing: "16",
        fontWeight: "normal",
        textAlign: "left",
        textColor: "warm"
      },
      currentParagraphIndex: 0,
      scrollTop: 0,
      currentListIndex: 0,
      currentLastListIndex: 0,
      currentScrollY: 0,
      touchStartX: 0,
      touchStartY: 0,
      touchMoved: false,
      measuringPage: false,
      positionReady: false,
      pageVisible: false,
      restoreTimer: null,
      importLoading: false,
      articleLoading: false,
      lastTouchPageAt: 0,
      lastLoadAt: 0,
      fontClass: "font-30",
      lineClass: "line-16",
      weightClass: "weight-normal",
      alignClass: "align-left",
      colorClass: "color-warm"
    };
  },
  onInit() {
    const requestedArticleId = this.articleId || "quiet-morning";
    if (requestedArticleId === IMPORTED_ARTICLE_ID) {
      this.prepareImportedArticle();
    } else {
      this.articleId = requestedArticleId;
      const cachedArticle = ARTICLE_CACHE[requestedArticleId];
      if (cachedArticle) {
        this.setArticleForDisplay(cachedArticle);
        this.articleId = cachedArticle.id;
        this.articleLoading = false;
        this.restoreTimer = setTimeout(() => {
          this.restoreTimer = null;
          this.restoreReadingPosition();
        }, 0);
      } else {
        const article = getArticle(requestedArticleId);
        ARTICLE_CACHE[requestedArticleId] = article;
        this.setArticleForDisplay(article);
        this.articleId = article.id;
        this.articleLoading = false;
        this.restoreTimer = setTimeout(() => {
          this.restoreTimer = null;
          this.restoreReadingPosition();
        }, 0);
      }
    }
    this.readerState = loadReaderState();
    this.currentParagraphIndex = Number(this.readerState.progress[this.articleId] || 0);
    this.applyFontState(this.readerState.fontSize);
    this.applyReadingSettings();
  },
  prepareImportedArticle() {
    this.importLoading = true;
    const metadata = getImportedArticleMetadata();
    this.articleId = IMPORTED_ARTICLE_ID;
    this.article = {
      id: IMPORTED_ARTICLE_ID,
      title: metadata ? metadata.title : "导入的 TXT",
      summary: metadata ? metadata.summary : "",
      readTime: metadata ? metadata.readTime : "",
      paragraphs: ["正在读取 TXT……"]
    };
    readImportedArticle(metadata, {
      success: (article) => {
        this.setArticleForDisplay(article);
        this.importLoading = false;
        if (this.restoreTimer) clearTimeout(this.restoreTimer);
        this.restoreTimer = setTimeout(() => {
          this.restoreTimer = null;
          this.restoreReadingPosition();
        }, 50);
      },
      fail: (data, code) => {
        console.warn("读取导入的 TXT 失败", data, code);
        this.importLoading = false;
        this.article.paragraphs = ["TXT 读取失败，请从手机重新传入。"];
      }
    });
  },
  onReady() {
    if (this.restoreTimer) clearTimeout(this.restoreTimer);
    this.restoreTimer = setTimeout(() => {
      this.restoreTimer = null;
      this.restoreReadingPosition();
    }, 80);
  },
  onShow() {
    this.pageVisible = true;
    const latestState = loadReaderState();
    this.readerState = latestState;
    this.applyFontState(latestState.fontSize);
    this.applyReadingSettings();
  },
  setArticleForDisplay(article) {
    const paragraphs = article && article.paragraphs ? article.paragraphs : [];
    this.article = {
      ...article,
      paragraphs
    };
    this.pendingParagraphs = [];
  },
  onReaderTouchStart(event) {
    const touch = event && event.touches && event.touches[0];
    this.touchMoved = false;
    if (touch) {
      this.touchStartX = touch.clientX;
      this.touchStartY = touch.clientY;
    }
  },
  onReaderTouchMove(event) {
    const touch = event && event.touches && event.touches[0];
    if (touch && (Math.abs(touch.clientX - this.touchStartX) > 10 || Math.abs(touch.clientY - this.touchStartY) > 10)) {
      this.touchMoved = true;
    }
  },
  onReaderTouchCancel() {
    this.touchMoved = true;
  },
  onReaderIndexChange(event) {
    const first = event && typeof event.first === "number" ? event.first : 0;
    const last = event && typeof event.last === "number" ? event.last : first;
    this.currentListIndex = Math.max(0, first);
    this.currentLastListIndex = Math.max(this.currentListIndex, last);
    this.currentParagraphIndex = Math.max(0, this.currentListIndex - 1);
    this.persistReadingPosition();
  },
  onReaderTouchEnd() {
    const now = Date.now();
    if (this.touchMoved || now - this.lastTouchPageAt < 300) return;
    this.lastTouchPageAt = now;
    this.turnPage(this.touchStartX);
  },
  onReaderClick(event) {
    if (Date.now() - this.lastTouchPageAt < 400) return;
    const x = event && typeof event.clientX === "number" ? event.clientX : event && typeof event.pageX === "number" ? event.pageX : -1;
    this.turnPage(x);
  },
  turnPage(x) {
    if (x < 0 || this.measuringPage || !this.positionReady) return;
    const readerList = this.$element("readerList");
    if (!readerList) return;
    const direction = x < 233 ? -1 : 1;
    const lastIndex = this.article.paragraphs.length + 1;
    const base = direction > 0 ? this.currentLastListIndex + 1 : this.currentListIndex - 1;
    const target = Math.max(0, Math.min(lastIndex, base));
    this.currentListIndex = target;
    this.currentLastListIndex = target;
    this.currentParagraphIndex = Math.max(0, target - 1);
    readerList.scrollTo({
      index: target
    });
    this.persistReadingPosition();
  },
  persistReadingPosition() {
    if (!this.positionReady) return;
    this.readerState.scrollProgress[this.articleId] = this.currentParagraphIndex;
    this.readerState.progress[this.articleId] = this.currentParagraphIndex;
    saveReaderState(this.readerState);
  },
  restoreReadingPosition() {
    if (this.articleId === IMPORTED_ARTICLE_ID && this.importLoading || this.articleLoading) return;
    const savedOffset = this.readerState.scrollProgress[this.articleId];
    if (typeof savedOffset === "number" && isFinite(savedOffset)) {
      this.currentParagraphIndex = Math.max(0, Math.floor(savedOffset));
      this.currentListIndex = this.currentParagraphIndex + 1;
      const readerList2 = this.$element("readerList");
      if (readerList2) readerList2.scrollTo({
        index: this.currentListIndex
      });
      this.positionReady = true;
      return;
    }
    const index = Math.min(this.currentParagraphIndex, this.article.paragraphs.length - 1);
    const readerList = this.$element("readerList");
    if (index > 0 && readerList) {
      this.currentListIndex = index + 1;
      readerList.scrollTo({
        index: this.currentListIndex
      });
      this.positionReady = true;
    } else {
      this.positionReady = true;
    }
  },
  applyFontState(fontSize) {
    this.readerState.fontSize = fontSize;
    this.fontClass = `font-${fontSize}`;
  },
  applyReadingSettings() {
    this.lineClass = `line-${this.readerState.lineSpacing}`;
    this.weightClass = `weight-${this.readerState.fontWeight}`;
    this.alignClass = `align-${this.readerState.textAlign}`;
    this.colorClass = `color-${this.readerState.textColor}`;
  },
  onHide() {
    this.persistReadingPosition();
    this.pageVisible = false;
  },
  onDestroy() {
    this.pageVisible = false;
    if (this.restoreTimer) clearTimeout(this.restoreTimer);
  }
};
$app_define$("@app-component/index", [], function($app_require$2, $app_exports$, $app_module$) {
  $app_module$.exports = $app_script$1182111904.default || $app_script$1182111904;
  $app_module$.exports.style = $app_style$1182111904;
});
$app_bootstrap$("@app-component/index");
//# debugId=eee7ea1e-f9ec-42c6-baa9-8b90ddcea5cf
//# sourceMappingURL=index.js.map
